---
docker_deps:
  - apt-transport-https
  - ca-certificates
  - curl
  - software-properties-common
  - python-software-properties

docker_ubuntu1404_deps:
  - "linux-image-extra-{{ ansible_kernel }}"
  - linux-image-extra-virtual

docker_package: docker-ce

docker_service: docker
